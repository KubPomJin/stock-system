# StockKeep — บันทึกการเปลี่ยนแปลง (Changelog)

## v1.5.5

**นำเข้ารายการสินค้าจากไฟล์ Excel ของ 4POS ได้ในโปรแกรม** (หน้าผู้ใช้งาน · แอดมินเท่านั้น)
- 4POS ยังใช้งานอยู่และมีสินค้าเพิ่มเรื่อยๆ จึงทำเป็นปุ่มถาวรในโปรแกรม ไม่ต้องเรียกสคริปต์ทุกครั้ง
- **รับทั้ง `.xls` และ `.xlsx`** — ไฟล์ที่ 4POS ส่งออกมาเป็น `.xls` แบบเก่า ใช้ได้เลยไม่ต้องแปลง
- เลือกไฟล์ → **ดูสรุปก่อนว่าจะเกิดอะไร** → ยืนยัน (รูปแบบเดียวกับหน้าเชื่อมระบบบิล)
- จับคู่ด้วย**บาร์โค้ด** · **ชื่อ/ราคา/ทุน/หน่วย ของที่มีอยู่แล้วไม่ถูกทับ** · เพิ่มเฉพาะตัวที่ยังไม่มี
- มีตารางเทียบชื่อคู่กันของบาร์โค้ดที่ตรงกันทั้งสองฝั่ง — กันกรณีรหัสสั้นๆ ที่เคยคีย์มือ
  ไปชนกับรหัสจริงของ 4POS แล้วกลายเป็นคนละสินค้า
- **ยอดคงเหลือเป็นตัวเลือก** ติ๊กแล้วเลือกสถานที่ → ตั้งยอดให้เท่ากับไฟล์ (ยอดเดิมถูกทับ)
  ลงเป็น movement ตามส่วนต่างเสมอ **ไม่มี UPDATE ยอด** ตามกติกา ledger ·
  นำเข้าไฟล์เดิมซ้ำยอดไม่บวกทบ · สินค้าที่ไม่มีในไฟล์ไม่ถูกแตะ
- เตือนถ้าไฟล์มี `Contain` ไม่เท่ากับ 1 เพราะระบบยังตั้งสินค้าเป็นหน่วยเดียว (BACKLOG 3.0)
- ตาราง `catalog_imports` เก็บประวัติการนำเข้า
- เพิ่ม dependency **SheetJS (xlsx 0.20.3)** ดึงจาก CDN ของผู้พัฒนา เพราะตัวบน npm ค้างที่ 0.18.5
  ซึ่งมีช่องโหว่ที่ยังไม่ได้แก้ · เป็น dependency ตัวที่ 3 ของโปรเจกต์

**ทดสอบแล้ว**
- ตัวอ่านไฟล์ให้ผลตรงกับไฟล์จริงทุกตัวเลข (4,660 แถว · ไม่มีปัญหา · บาร์โค้ดเลขนำหน้าศูนย์ 807 รายการอยู่ครบ ·
  หมวดหมู่ 41 · หน่วย 32 · ยอดไม่เป็นศูนย์ 3,730 · ติดลบ 171) และหาหัวตารางเองได้ถูก
- `xlsx` ถูกแพ็กเข้า `app.asar` ของ installer เรียบร้อย
- `npx tsc --noEmit` ผ่าน
- **ใช้งานจริงกับไฟล์ 4POS แล้ว** — นำเข้าสินค้า 4,659 รายการ + หมวดหมู่ 41 + หน่วย 26
  แล้วนำเข้ารอบสองเพื่อลงยอดคงเหลือที่คลังสินค้า 3,731 รายการ (OPENING 3,730 / ADJUST 1)
  ยอดติดลบ 171 รายการตรงกับไฟล์ · รอบสองไม่สร้างสินค้าซ้ำ (added 0) · สินค้าที่ไม่มีในไฟล์ไม่ถูกแตะ
- ⚠️ เคสบาร์โค้ดชนเกิดขึ้นจริง 1 ราย: `000002` ในระบบคือ *ปูนช้าง* (นับไว้ 10) แต่ในไฟล์คือ
  *(X)ข้ออ้อย 10 มิล SD-40* ยอด 0 → ปูนช้างถูกปรับเป็น 0 · แก้ได้ที่หน้าสินค้า และเห็นร่องรอยครบใน ledger

## v1.5.4

**ผังโกดัง — หมุนผังให้ตรงกับที่เห็นตอนยืนอยู่หน้าประตู**
- ประตู 2–5 ย้ายลง **แถวล่าง** · Office มุม **ซ้ายล่าง** · โซน 6–13 ขึ้น **แถวบน** (เรียง 6→13 ซ้ายไปขวา) ·
  โซน 1–5 ย้ายไปเรียงลง **ผนังซ้าย** (5 บนสุด → 1 ล่างสุด ใกล้ออฟฟิศ) · Back Door มุม **ขวาบน**
- เป็นผังเดิมหมุน 180° ไม่ได้เปลี่ยนจำนวนหรือชื่อโซน — ของเดิมวางตามกระดาษที่วาดไว้
  ซึ่งอ่านถูกบนกระดาษ แต่กลับด้านกับที่คนยืนหน้าประตูเห็นจริง (ซ้ายบนจอ = ขวาในโกดัง)
- เครื่องที่ติดตั้งไว้แล้วย้ายตำแหน่งให้เองตอนเปิดโปรแกรม (`migrateZoneLayout()` ใน `database.ts`)
  โดย **UPDATE ทับตำแหน่งเดิม ไม่ลบแถวโซนทิ้ง** — สินค้าที่ผูกโซนไว้แล้วใน `product_zones` จึงไม่หาย
  ใช้ `BACKDOOR.grid_row` เป็นตัวบอกว่าผังหันทางไหน จึงรันซ้ำทุกครั้งที่เปิดโปรแกรมได้โดยทำงานจริงแค่ครั้งเดียว
- ทดสอบกับ **สำเนาฐานข้อมูลจริง** แล้ว: โซนครบ 25 ช่อง · `product_zones` 7 แถวเท่าเดิมทุกแถว ·
  เปิดซ้ำแล้ว migration ไม่ทำงานซ้ำ · **เจ้าของร้านยืนยันผังในโปรแกรมจริงแล้วว่าถูกต้อง**

**หน้าสินค้า — กล่องเลือกคอลัมน์ถูกขอบพาเนลตัด กดไม่ถึงตัวเลือกที่เหลือ**
- `.panel` มี `overflow:hidden` (ไว้ตัดมุมตารางให้โค้งตามพาเนล) ซึ่งตัดเมนูที่เป็น `position:absolute` ทิ้ง
  ตอนมีสินค้าน้อย พาเนลเตี้ย เลยเห็นตัวเลือกแค่ 3 อันครึ่งจากทั้งหมด 10 อัน — สต๊อก / แยกตามหน่วย /
  ต้นทุน / ราคาขาย / ราคาช่าง / ราคาส่ง / สถานะ **กดไม่ถึงเลย**
- แก้เป็น `position:fixed` แล้วคำนวณตำแหน่งจากปุ่มตอนเปิด (`placeColPicker()`) จำกัดความสูงไม่ให้ล้นจอ
  และปิดเมื่อคลิกที่อื่น / เลื่อนหน้า / ย่อขยายหน้าต่าง — **ไม่ได้แตะ `.panel`** ที่ทุกหน้าใช้ร่วมกัน

> `npx tsc --noEmit` ผ่าน · build เป็น `release/StockKeep Setup 1.5.4.exe` แล้ว
> (กล่องเลือกคอลัมน์แก้ไปหลังจากที่เจ้าของรันดู ยังไม่ได้เห็นในโปรแกรมจริง — อยู่ใน installer ตัวนี้แล้ว)

## v1.5.3

**เอาช่อง "ปรับความสูงหน้ากระดาษ" ออก**
- ช่องนี้เพิ่มเข้ามาใน v1.5.2 เพื่อชดเชยอาการกระดาษเลื่อนสะสม แต่เจ้าของร้านเลือกไม่ใช้
  หน้าต่างพิมพ์จึงกลับมาเหลือเฉพาะสิ่งที่ใช้จริง: เครื่องพิมพ์ · จำนวนสำเนา ·
  ขนาดกระดาษ (9/8 นิ้ว) · ระยะขอบ 4 ด้าน
- ความสูงหน้ากลับไปเป็น **5.5 นิ้วคงที่** ตามขนาดกระดาษจริง
- **อาการเลื่อนสะสมยังไม่หาย** — ต้องแก้ที่ตัวเครื่อง LQ-310 (form length /
  skip over perforation) ซึ่งซอฟต์แวร์อ่านค่าและควบคุมไม่ได้
  ยืนยันแล้วว่าไฟล์ที่โปรแกรมส่งออกไม่มีการเลื่อนเลย (10 แผ่น MediaBox เท่ากันหมด)


## v1.5.2

**🐞 เลขใบสั่งสินค้าถูกใช้ทิ้งตอนกด "ดูตัวอย่าง"**
- เดิมกดดูตัวอย่างแล้วเลขถูกจองทันที ปิดหน้าต่างโดยไม่พิมพ์ = **เลขหายไปเลย**
  ไม่มีอะไรเตือน และเอากลับมาไม่ได้ → เกิดช่องว่างในเล่มเอกสารกระดาษที่อธิบายทีหลังไม่ได้
- ตอนนี้ **เลขถูกใช้จริงตอนกด "พิมพ์" เท่านั้น** · ดูตัวอย่างแล้วปิดทิ้งได้ไม่เสียเลข
- ตัวอย่างคำนวณเลขฝั่งหน้าจอเพื่อแสดงให้ดู แต่**เลขที่พิมพ์จริงมาจากเซิร์ฟเวอร์เสมอ**
  แล้ววาดใบใหม่ตามเลขที่ได้จริงก่อนส่งเครื่องพิมพ์ — ตัวเลขบนกระดาษจึงไม่มีทางคลาดจากที่จองไว้
- ตรวจแล้วว่ารูปแบบเลขฝั่งหน้าจอตรงกับฝั่งเซิร์ฟเวอร์ทุกกรณี รวมถึงตอนขึ้นหลัก (0998 → 1000)

**ปรับความสูงหน้ากระดาษได้ (แก้อาการพิมพ์ยาวแล้วเลื่อนลงสะสม)**
- เพิ่มช่อง **"ปรับความสูงหน้ากระดาษ (มม.)"** ในหน้าต่างพิมพ์ใบสั่งสินค้า **จำไว้ต่อเครื่อง**
- ใช้เมื่อพิมพ์ยาวหลายใบแล้วฟอร์มค่อยๆ เลื่อนลง — เกิดจากความยาวที่เครื่องเดินกระดาษจริง
  ไม่ตรงกับหน้าที่เราส่งไป ซอฟต์แวร์อ่านค่านั้นไม่ได้ แต่**ส่งหน้าที่สั้นลงเท่ากันไปหักล้างได้**
- วิธีหาค่า: **ระยะที่เลื่อน ÷ จำนวนใบ** แล้วใส่เป็นค่าลบ (เช่น เลื่อนลง 20 มม. ใน 10 ใบ → ใส่ −2)
- ความสูงแผ่นหดตามหน้าอัตโนมัติ จึงยังได้ **1 ใบ = 1 หน้า** เสมอ
- ตรวจด้วย `printToPDF` 4 ค่า (0 / −2 / −0.5 / +1 มม.) ขนาดหน้าใน PDF ตรงตามที่ตั้งทุกครั้ง
- หมายเหตุ: ยืนยันแล้วว่า**ไฟล์ที่โปรแกรมส่งออกไม่มีการเลื่อนเลย** (10 แผ่น MediaBox เท่ากันหมด
  ขอบสมมาตรทุกแผ่น) อาการเลื่อนเป็นเรื่องกลไกของเครื่องพิมพ์ล้วนๆ ช่องนี้คือตัวชดเชย
  ไม่ใช่การแก้บั๊ก — ถ้าวันไหนตั้ง form length ที่ตัวเครื่องถูกแล้ว ให้ตั้งกลับเป็น 0


## v1.5.1

**เก็บ "จำนวนที่นับได้จริงแยกตามหน่วย" ลงฐานข้อมูล**
- v1.5.0 ให้กรอกแยกหน่วยได้ (4 กล่อง + 2 ชิ้น) แต่**จำไว้แค่ในหน้าจอ** พอกดบันทึกจะแปลงเป็น
  หน่วยฐานแล้วทิ้งรายละเอียด ย้อนดูทีหลังเห็นแค่ตัวเลขที่หารกลับ ไม่ใช่ที่นับจริง
- ตอนนี้เก็บสิ่งที่นับไว้ตามที่เขียน (`stocktake_lines.counted_parts` เป็น JSON)
  **"4 กล่อง + 2 ชิ้น" กับ "202 ชิ้น" เป็นคนละการนับ** ยอดรวมเท่ากันก็จริงแต่ของบนชั้นไม่เหมือนกัน
- หน้าประวัติ (ทั้งแบบตาราง แบบรายการ และ PDF) แสดงตามที่นับจริง
  รอบเก่าที่ยังไม่มีข้อมูลนี้จะแสดงยอดรวมเหมือนเดิม ไม่พัง
- ทดสอบกับสำเนาฐานข้อมูลจริง: migration เพิ่มคอลัมน์ได้ · JSON เก็บ-อ่านกลับตรงกัน ·
  ผลรวมของหน่วยตรงกับยอดฐานที่บันทึกไว้ · แถวเก่าที่ไม่มีข้อมูลยังแสดงถูก

> **บันทึกการตัดสินใจ:** เจ้าของร้านยืนยันว่าจำนวนแต่ละหน่วยคือ**ของที่นับเห็นจริง**
> ไม่ใช่การหารจากยอดรวม (มีกล่องที่ยังไม่แกะ กับของที่แกะขายเป็นชิ้นแล้ว)
> แต่เลือกให้ระบบ**เก็บแยกเฉพาะตอนตรวจนับ** ไม่ติดตามจำนวนกล่องแบบเรียลไทม์
> เพราะนั่นจะต้องให้คนหน้าร้านกดบันทึกทุกครั้งที่แกะกล่อง ซึ่งไม่ไหวในทางปฏิบัติ
> ยอดคงเหลือจึงยังเป็นหน่วยฐานตามกติกา ledger เดิมทุกประการ


## v1.5.0

**หน้าประวัติตรวจนับ — ดูคู่กัน / ออก PDF / แก้ไขย้อนหลังได้**
- **แบบตาราง (เหมือนตอนคีย์)** — เดิมคลังกับหน้าร้านแยกเป็นคนละแถว ตอนนี้อยู่**คู่กันในแถวเดียว**
  ต่อสินค้าหนึ่งชิ้น มีคอลัมน์ ในระบบ / นับได้ / ผลต่าง ของแต่ละที่ พร้อมยอดรวม
  สลับกลับไปดู **แบบรายการ** เดิมได้ (แบบรายการยังเป็นที่เดียวที่เห็นหมายเหตุและของเข้าครบ)
- **บันทึกเป็น PDF** — ออกเป็นแฟ้มบันทึกผลการตรวจนับ A4 แนวนอน ใช้ `printToPDF`
  จึงไม่ต้องมีเครื่องพิมพ์ และไม่เจอปัญหาไฟล์ 0 ไบต์
- **แก้ไขตัวเลขที่คีย์ผิดได้** — กด "แก้ไขตัวเลข" แล้วพิมพ์ทับในช่อง "นับได้"

**การแก้ไขไม่เขียนทับของเดิม (สำคัญ)**
- ระบบสร้าง **รอบใหม่ที่ชี้กลับไปยังรอบเดิม** (`amends_session_id`) แล้วลง `ADJUST`
  เท่ากับ **(จำนวนใหม่ − จำนวนที่เคยนับ)** — ไม่ใช่เทียบกับยอดปัจจุบัน
  ของที่เคลื่อนไหวหลังจากรอบนั้นจึงไม่ถูกลบผลทิ้ง
- **รอบเดิมยังอยู่ครบไม่ถูกแตะ** — ย้อนดูได้ทั้ง "ตอนนั้นนับได้เท่าไร" และ "ตอนนี้เชื่อว่าเท่าไร"
  พร้อมชื่อคนแก้และเวลา
- เหตุผล: โปรแกรมนี้ถูกสร้างมาแทน 4POS ที่แก้ยอดทับได้จนติดลบโดยไม่มีประวัติ
  ถ้าเปิดให้แก้ทับ ปัญหาเดิมจะกลับมาทางประตูหลัง
- ทดสอบกับสำเนาฐานข้อมูลจริงแล้ว ผ่านครบ 5 ข้อ: บรรทัดเดิมไม่เปลี่ยน · รอบเดิมไม่เปลี่ยน ·
  รอบใหม่เชื่อมกลับถูก · **ยอดขยับเท่ากับส่วนต่างพอดี** (100 → 107 เมื่อแก้จาก 100 เป็น 107) ·
  รอบเดิมยังอยู่ในประวัติ

**ฐานข้อมูล** — เพิ่มคอลัมน์ `stocktake_sessions.amends_session_id` (migration อัตโนมัติ ข้อมูลเดิมไม่หาย)

**แก้จำนวนคงเหลือได้จากหน้าสินค้า — กรอกแยกทีละหน่วย**
- เดิมช่องจำนวนโผล่เฉพาะตอน**สร้างสินค้าใหม่** พอกดแก้ไขสินค้าเดิมจะใส่จำนวนไม่ได้เลย
  มีแค่ขั้นต่ำกับสูงสุด · ตอนนี้**แก้จำนวนคงเหลือได้ตลอด** แยกตามสถานที่เก็บ
- **ช่องกรอกแยกตามหน่วย** เหมือนหน้าตรวจนับ — สินค้าที่มี ถุง/ชิ้น/กล่อง จะได้ช่องละหน่วย
  ในแต่ละสถานที่ ระบบเติมยอดปัจจุบันที่แตกเป็นหน่วยแล้วมาให้ แก้ช่องไหนก็ได้
- ช่องจะปรับตามโครงสร้างหน่วยทันทีที่แก้ระดับหน่วยด้านบน
- **เรียงหน่วยจากใหญ่ไปเล็กให้อัตโนมัติตอนบันทึก** — เดิมถ้ากรอกสลับลำดับ (เช่น ถุง ×200
  อยู่ใต้ ชิ้น ×50) การแยกหน่วยจะคำนวณผิดเงียบๆ เพราะระบบไล่หารจากบนลงล่าง
- ทดสอบการแตกหน่วยแล้วรวมกลับ 39 กรณี (3 โครงสร้างหน่วย × 13 จำนวน) **ตรงทุกกรณี ไม่มีเศษหาย**
- **ไม่ได้เขียนทับยอด** — ระบบอ่านยอดจาก ledger แล้วลงรายการ `ADJUST` เท่ากับ**ส่วนต่าง**
  (หมายเหตุ "ปรับจำนวนจากหน้าสินค้า" + ชื่อคนแก้) ยอดคงเหลือยังเป็น `SUM(qty_change)` เหมือนเดิม
  และย้อนดูได้ว่าใครแก้อะไรเมื่อไร
- ถ้าตัวเลขไม่เปลี่ยน **จะไม่สร้างรายการเปล่าทิ้งไว้**
- ทดสอบกับสำเนาฐานข้อมูลจริงแล้ว 4 กรณี (100→37, 37→37, 37→0, 0→12) ยอดตรงทุกครั้ง
  และเกิดรายการเคลื่อนไหวแค่ 3 แถวตามที่ควรเป็น

**หน้าสินค้า — เลือกคอลัมน์เองได้ + ตรึงชื่อสินค้า**
- ปุ่ม **"คอลัมน์"** ติ๊กเลือกได้ว่าจะดูอะไร (โซน/หมวดหมู่/หน่วย/สต๊อก/ต้นทุน/ราคาแต่ละแบบ/สถานะ)
  **จำค่าไว้ให้ต่อเครื่อง** · ค่าเริ่มต้นปิดต้นทุนกับราคาช่าง/ราคาส่งไว้ เพราะเป็นตัวที่ทำให้ตารางล้นที่สุด
- **บาร์โค้ดกับคำอธิบายถูกตรึงไว้ซ้ายมือ** — เลื่อนตารางไปขวาแค่ไหนก็ยังรู้ว่าแถวนั้นคือสินค้าอะไร
  (เดิมเลื่อนแล้วเห็นแต่ตัวเลข ไม่รู้ว่าของใคร)
- คอลัมน์ใหม่ **"แยกตามหน่วย"** แสดงทุกระดับหน่วยทั้ง**จำนวนและราคา**
  (เช่น `กล่อง 4 · ถุง 2 · ชิ้น 98` / `กล่อง 360.00 · ถุง 90.00 · ชิ้น 15.00`)
  ราคาต่อหน่วย = ราคาต่อหน่วยฐาน × จำนวนหน่วยฐานในหน่วยนั้น
- ตัวเลือกคอลัมน์ยัง**กรองตามสิทธิ์**เหมือนเดิม — พนักงานไม่เห็นแม้แต่ตัวเลือกของราคาที่ไม่มีสิทธิ์ดู

**ตรวจนับสต๊อก — กรอกแยกตามหน่วยได้แล้ว**
- สินค้าที่มีหลายระดับหน่วย (เช่น กล่อง ×24 / ชิ้น ×1) จะได้ **ช่องกรอกแยกทีละหน่วย
  ในแต่ละคลัง** — มี 4 กล่องกับอีก 2 ชิ้น ก็พิมพ์ 4 กับ 2 ตรงๆ ระบบคูณรวมให้เอง
  ไม่ต้องคิดเลข 98 ในหัวแล้วค่อยพิมพ์
- ระบบ**จำสิ่งที่พิมพ์ไว้แยกตามหน่วย** ไม่ยุบเป็นตัวเลขเดียว — กลับมาดูยังเห็น "4 / 2" เหมือนเดิม
- สินค้าที่มีหน่วยเดียวยังเป็นช่องเดียวเหมือนเดิม ไม่มีอะไรเปลี่ยน
- กด Enter ยังไล่ช่องต่อเนื่องได้เหมือนเดิม (กล่อง → ชิ้น → คลังถัดไป)
- เว้นว่างทุกช่อง = **ยังไม่นับ** ซึ่งไม่เท่ากับนับได้ 0 — แยกกันชัดเจนเหมือนเดิม

**เลือกหลายรายการเพื่อจัดเข้าโซนทีเดียว**
- หน้าสินค้ามี**ช่องติ๊กหน้าแถว** (คนจัดสต็อกขึ้นไป) + ติ๊กหัวตารางเพื่อเลือกทั้งหมดที่เห็น
  — เลือกเฉพาะแถวที่ผ่านตัวกรอง ไม่ไปโดนแถวที่ซ่อนอยู่
- แถบเครื่องมือโผล่เมื่อเลือกแล้ว: เลือกโซน → **เพิ่มลงโซน** / **เอาออกจากโซน**
- การเลือก**ไม่หายเมื่อเปลี่ยนตัวกรอง** — กรองหลายรอบแล้วค่อยๆ สะสมรายการได้
- ทำงานเป็น transaction เดียว: รหัสผิดกลางทาง = ไม่มีการเปลี่ยนแปลงค้างครึ่งๆ
  และรายงานจำนวนที่ข้าม (อยู่ในโซนนั้นอยู่แล้ว) แยกจากจำนวนที่เปลี่ยนจริง

**ฟอร์มสินค้าใช้ง่ายขึ้น**
- เปลี่ยนจากแถบด้านขวา 460px เป็น **หน้าต่างกลางจอกว้าง 2 คอลัมน์** เห็นทุกช่องพร้อมกัน
  ไม่ต้องเลื่อนขึ้นลงเทียบข้อมูล · โครงสร้างหน่วยกับสต๊อกเริ่มต้นกินเต็มความกว้าง
  เพราะเป็นรายการหลายแถว · จอเล็กยุบเป็นคอลัมน์เดียวอัตโนมัติ
- ช่องชื่อหน่วยมี**รายการหน่วยที่เคยใช้ให้เลือก** กันพิมพ์ไม่ตรงกัน และยังพิมพ์ชื่อใหม่ได้
  (ดึงจากข้อมูลที่โหลดอยู่แล้ว ไม่เพิ่มการเรียกข้อมูล)

**เลขเวอร์ชันในโปรแกรมดึงจาก package.json**
- เดิมพิมพ์ตายไว้ที่มุมซ้ายล่างและค้างอยู่ที่ v1.4.1 · ตอนนี้ฝังตอน build
  จึงตรงกับเวอร์ชันที่ปล่อยจริงเสมอ

**ระบุโซนได้จากหน้าสินค้า + เรียง/กรองตามโซน**
- ฟอร์ม **เพิ่ม/แก้สินค้า** มีช่อง **"โซนที่เก็บในโกดัง (ไม่บังคับ)"** เป็นช่องติ๊กเลขโซน
  **เว้นว่างไว้ได้** ถ้ายังไม่ได้จัดที่ · ติ๊กได้มากกว่าหนึ่งโซนถ้าของแยกเก็บหลายที่
  (ใช้ช่องติ๊กไม่ใช่ dropdown เพราะ dropdown เดี่ยวจะทำให้โซนที่สองหายเงียบๆ ตอนบันทึก)
- ตารางสินค้าเพิ่ม **คอลัมน์ "โซน"** — สินค้าที่ยังไม่ระบุจะขึ้น "—"
- **กรองตามโซน** ด้วยแถบชิปใหม่ (รวมตัวเลือก "ยังไม่ระบุ" เพื่อไล่เก็บของที่ยังไม่ได้จัดที่)
- **เรียงตามโซน (1 → 13)** ในตัวเลือกการเรียง — เรียงตามลำดับที่คนเดินหยิบของจริง
  ของที่ยังไม่ระบุโซนไปอยู่ท้ายสุด ไม่ใช่ถูกนับเป็นโซน 0
- ส่ง `zoneIds` เป็น array ชัดเจนทุกครั้งที่บันทึก → **ติ๊กออกหมดแล้วโซนถูกล้างจริง**
  (ถ้าไม่ส่ง field นี้เลย ระบบจะไม่แตะโซนเดิม — ใช้กับการบันทึกจากที่อื่น)

**หน้าใหม่: ผังโกดัง + ค้นหาที่เก็บสินค้า** (สิทธิ์: พนักงานขึ้นไป)

ตอบคำถาม "ของชิ้นนี้อยู่ตรงไหนในโกดัง" ซึ่งเดิมต้องเดินหาหรือถามคนอื่น

- **ช่องค้นหา** — พิมพ์ชื่อสินค้าหรือบาร์โค้ด แล้วบอกทันทีว่าอยู่ **โซนไหน** (บอกได้หลายโซน
  ถ้าของกระจายหลายที่) · สินค้าที่ยังไม่ได้ระบุโซนจะขึ้นว่า "ยังไม่ได้ระบุโซน" ให้เห็นชัด
- **ผังโกดังกดได้** — วาดตามผังจริง: ประตู 2–5 แถวบน, Office มุมขวา, โซน 1–5 เรียงลงผนังขวา,
  โซน 6–13 เรียงตามแถวล่าง, Back Door ซ้ายล่าง · **กดที่โซนแล้วโชว์รายการสินค้าทั้งหมดในโซนนั้น**
- **ค้นหาแล้วผังไฮไลต์ให้** — โซนที่มีของที่ค้นหาจะเปลี่ยนสี เห็นเลยว่าต้องเดินไปทางไหน
- **เพิ่ม/เอาสินค้าออกจากโซน** ได้จากหน้าเดียวกัน (สิทธิ์: คนจัดสต็อกขึ้นไป) —
  เลือกโซนก่อนแล้วค่อยเพิ่มสินค้าเข้าไป เร็วกว่าไปแก้ทีละสินค้า
- ใส่ **หมายเหตุของโซน** ได้ เช่น "เหล็กเส้นยาว 6 เมตร"

**ฐานข้อมูล** — เพิ่มตาราง `zones` และ `product_zones` (migration อัตโนมัติตอนเปิดโปรแกรม
ข้อมูลเดิมไม่หาย) พร้อม seed ผังตามที่เจ้าของร้านวาดไว้: โซนเก็บของ 13 ช่อง + ป้าย 6 ช่อง

**การออกแบบที่ตั้งใจไว้ (อย่าเปลี่ยนโดยไม่คิดให้ครบ):**
- **โซนไม่ใช่มิติของสต๊อก** — เป็นแค่ตัวช่วยหาของ `stock_movements` ไม่ได้แตะเลย
  ยอดคงเหลือยังนับตาม `locations` (คลังสินค้า / หน้าร้าน) เหมือนเดิม กติกา ledger ไม่เปลี่ยน
- **หน้านี้ไม่ส่งราคาหรือต้นทุนผ่าน IPC เลย** เพราะพนักงาน (level 1) ใช้หน้านี้ได้
  คงกติกาการกรองราคาตาม role ไว้ครบ
- ตำแหน่งช่องบนผังเก็บใน DB (`grid_col/row/w/h`) ไม่ได้ฝังในโค้ด — ปรับผังทีหลังได้โดยไม่ต้องแก้โปรแกรม
- ผูกสินค้ากับโซนแบบ **many-to-many** เพราะของบางอย่างแยกเก็บสองที่

> ⚠️ **ยังไม่มีข้อมูลสินค้าในโซน** — ต้องกรอกว่าสินค้าไหนอยู่โซนไหนก่อนจึงจะค้นหาเจอ
> (ข้อมูลอยู่บนกระดาษที่เจ้าของร้านเขียนไว้ ยังไม่ได้ส่งเข้ามา)

## v1.4.3

**เลือกขนาดกระดาษที่ส่งให้เครื่องพิมพ์ได้ (ใบสั่งสินค้า, ต่อเครื่อง)**
- เพิ่มตัวเลือก **9 × 5.5 นิ้ว (กระดาษเต็มแผ่น)** / **8 × 5.5 นิ้ว (เฉพาะพื้นที่พิมพ์)**
- เหตุผล: ไดรเวอร์บางตัวนับ "หน้ากระดาษ" เป็น**พื้นที่ที่พิมพ์ได้** ไม่ใช่กระดาษจริง
  ถ้าเป็นแบบนั้น การส่ง 9 นิ้วจะทำให้ฟอร์มเยื้องขวาไปเท่ากับความกว้างแถบรูฟันเฟือง
  (0.5 นิ้ว) บวกกับขอบ 0.5 นิ้วของเราเอง = **เยื้อง 1 นิ้ว** ตรงกับที่วัดได้จริงที่หน้างาน
- โหมด 8 นิ้วจะไม่ใส่ขอบซ้าย-ขวาเอง เพราะไดรเวอร์เริ่มพิมพ์ที่ขอบในของแถบรูอยู่แล้ว
  ใส่เพิ่มจะกลายเป็นขอบซ้อนสองชั้น
- ตรวจแล้วทั้งสองโหมด: MediaBox ออกมา **9.00 × 5.50** และ **8.00 × 5.50** ตามที่เลือก
  หน้าละ 1 แผ่นเท่ากัน

**🐞 เส้นกรอบขวาของตารางใบสั่งสินค้าไม่พิมพ์ออกมา**
- ตารางใช้ `border-collapse:collapse` ซึ่งวาด**เส้นกรอบนอกคร่อมขอบตาราง** เห็นจริงแค่ครึ่งเดียว
  ค่าที่ตั้งไว้ 1.5px จึงเรนเดอร์ออกมาเป็น **0.8px** = ประมาณ 1.5 จุดที่ 180 dpi ของ LQ-310
  บางเกินกว่าหัวเข็มจะตีติด (เส้นในตารางไม่มีปัญหาเพราะสองเซลล์ใช้เส้นร่วมกัน ได้ 1.5px เต็ม)
- เส้นซ้ายรอด เส้นขวาหาย เพราะการปัดเศษ sub-pixel ตกคนละทาง — ข้างหนึ่งปัดขึ้นเป็น 1 จุด
  อีกข้างปัดลงเป็น 0
- แก้โดยตั้งกรอบนอกเป็น **3px** เพื่อให้ครึ่งที่มองเห็นได้ 1.5px เท่ากับเส้นใน
  (วัดแล้ว: 0.8px → **2.4px**)
- ⚠️ ถ้าตั้งระยะขอบขวาเป็น 0 ครึ่งนอกของกรอบจะถูก `overflow:hidden` ตัด — เว้นไว้อย่างน้อย 2 มม.

**ตั้งระยะขอบกระดาษเองได้ทั้ง 4 ด้าน (ใบสั่งสินค้า, ต่อเครื่อง)**
- ช่องกรอก **บน / ล่าง / ซ้าย / ขวา** เป็นมิลลิเมตร ปรับแล้วเห็นผลในตัวอย่างทันที
  พร้อมปุ่ม **คืนค่าเริ่มต้น** และตัวเลขบอก**พื้นที่พิมพ์ที่เหลือจริง** (กว้าง × สูง มม.)
- **จำไว้เฉพาะเครื่องนั้น** — กระดาษต่อเนื่องไม่เคยอยู่ตำแหน่งเดียวกันในสองเครื่อง
  ระยะขอบจึงเป็นคุณสมบัติของเครื่องพิมพ์ ไม่ใช่ของเอกสาร ไม่ฝังลงเลย์เอาต์
- ค่าเริ่มต้น: บน-ล่าง 5.1 มม. (0.2 นิ้ว) · ซ้าย-ขวา 12.7 มม. (0.5 นิ้ว) สำหรับกระดาษเต็มแผ่น 9 นิ้ว
  และ 0 สำหรับโหมด 8 นิ้ว · เปลี่ยนโหมดกระดาษแล้วระยะขอบจะรีเซ็ตให้เหมาะกับโหมดนั้น
- ตรวจแล้ว 4 ชุดค่า: ระยะที่เรนเดอร์ตรงกับที่ตั้งทุกด้าน และความกว้างเนื้อหา
  เท่ากับ (กระดาษ − ซ้าย − ขวา) เป๊ะทุกกรณี
- แทนที่ช่อง "เลื่อนซ้าย-ขวา" ที่ทำไว้ก่อนหน้า ซึ่งใช้ padding เป็นกลไก จึง**ติดลบไม่ได้** —
  ในโหมด 8 นิ้วที่ขอบเป็น 0 อยู่แล้ว การใส่ค่าบวกจึงไม่มีผลใดๆ
- หมายเหตุ: ตัวเอกสารเองถูกต้องอยู่แล้ว — PDF ที่ส่งออกมี MediaBox
  **648 × 396 pt = 9.00 × 5.50 นิ้ว** เป๊ะ และขอบซ้าย-ขวาสมมาตร

**🐞 ใบตรวจนับกินกระดาษสองแผ่นต่อหนึ่งใบ (ต้นเหตุหลัก)**
- กฎ CSS ตอนพิมพ์ override `width` / `min-height` / `padding` / `margin` ของ `.pt-sheet`
  ไว้ครบ **แต่ลืม `height`** — กฎสำหรับแสดงบนจอตั้ง `height:210mm` (เต็มหน้า A4) ไว้
  ขณะที่พื้นที่พิมพ์จริงมีแค่ **190 มม.** (210 หัก @page margin 10 มม. สองด้าน)
  ทุกแผ่นจึงล้นไปหน้าที่สองที่แทบว่างเปล่า **1 ใบ = 2 หน้าเสมอ**
- วัดด้วย `printToPDF` ก่อนแก้ = 2 หน้าต่อแผ่น, หลังแก้ = **1 หน้าต่อแผ่น**
  (ใบสั่งสินค้าเป็นตัวควบคุม ได้ 1 หน้าทั้งก่อนและหลัง)
- อาการนี้ยังทำให้ `pageRanges` ที่คำนวณจากจำนวนแผ่นไม่ตรงกับจำนวนหน้าจริง
  ซึ่งเป็นเหตุให้งานพิมพ์ล้มเหลวหรือได้ไฟล์เปล่า 0 ไบต์

**🐞 แก้ "พิมพ์ใบตรวจนับไม่ได้ / Print job failed"**
- **จำเครื่องพิมพ์แยกตามชนิดเอกสาร** — ต้นเหตุคือหน้าต่างพิมพ์ทั้งสองแบบเลือก
  *เครื่องพิมพ์เริ่มต้นของ Windows* ให้เหมือนกัน ซึ่งที่ร้านคือ **EPSON LQ-310**
  ที่ใส่กระดาษต่อเนื่อง 9 × 5.5 นิ้วและ**ไม่มีกระดาษ A4 เลย** ใบตรวจนับที่เป็น A4
  จึงถูกยิงเข้าเครื่องนั้นทุกครั้งแล้วโดน driver ปฏิเสธ
  ตอนนี้ใบตรวจนับกับใบสั่งสินค้าจำเครื่องพิมพ์ของตัวเองแยกกัน เลือกครั้งเดียวแล้วจำให้
- **ข้อความ error บอกสาเหตุจริง** — เดิมขึ้นแค่ "Print job failed" (สตริงของ Chromium)
  ซึ่งไล่ต่อไม่ได้เลย ตอนนี้บอก**ชื่อเครื่องพิมพ์ + ขนาดกระดาษที่ขอ** และแนะให้เลือก
  เครื่องพิมพ์ให้ตรงชนิดเอกสาร

**`margins` เปลี่ยนเป็นค่าต่องานพิมพ์ (แก้บั๊กแฝงจาก v1.4.2)**
- v1.4.2 ใส่ `margins: { marginType: 'none' }` ไว้ที่ IPC handler กลาง ซึ่งมีผลกับ**ทุกงานพิมพ์**
  การขอ margin ศูนย์บน A4 = ขอพิมพ์ไร้ขอบ ซึ่งเครื่องพิมพ์ทั่วไปไม่รองรับ
- ตอนนี้ส่งเฉพาะใบสั่งสินค้า (dot matrix ที่ตั้ง margin 0.00 ไว้จริงใน driver)
  ใบตรวจนับกลับไปใช้ค่าปกติเหมือน v1.4.1 · เพิ่มคอมเมนต์กำกับทั้ง 3 ไฟล์ว่า
  **ห้ามตั้ง `'none'` เป็นค่าเริ่มต้น**
- หมายเหตุ: ข้อนี้เป็นบั๊กแฝงจริง แต่**ไม่ใช่ต้นเหตุของ Print job failed** —
  ทดสอบแล้วแก้ข้อนี้อย่างเดียวอาการยังอยู่

**หมายเหตุเรื่องพิมพ์ใบสั่งสินค้าบนเครื่องอื่น**
- ถ้าเนื้อหาเลื่อนไปทางขวาจนขอบขวาหาย แปลว่าเครื่องนั้น**ยังไม่ได้สร้างกระดาษ
  `Carbonless papaer 9x5.5`** ในไดรเวอร์ — ขนาดกระดาษที่ตั้งเองเป็นค่าเฉพาะเครื่อง
  ไม่ติดไปกับตัวโปรแกรม ต้องสร้างใหม่ทุกเครื่องที่จะพิมพ์ฟอร์มนี้
  (ค่าที่ถูกต้อง: 22.86 × 13.97 ซม. = 9.00 × 5.50 นิ้ว, margin 0.00 ทุกด้าน)

## v1.4.2

**🐞 แก้บั๊กใบสั่งสินค้าเลื่อนลงเรื่อยๆ ตอนพิมพ์ต่อกันหลายใบ**
- ต้นเหตุ: โปรแกรมบอกเครื่องพิมพ์ว่ากระดาษ **8 × 5.5 นิ้ว** ซึ่ง**ไม่ตรงกับฟอร์มใน driver**
  (`Carbonless papaer 9x5.5` = 9 × 5.5 นิ้ว) Windows หาฟอร์มไม่เจอเลยไปหยิบขนาดอื่นมาแทน
  ความยาวหน้าที่เครื่องเดินจริงจึงไม่ใช่ 5.5 นิ้ว → คลาดสะสมทีละนิดทุกใบ จน 24 ใบเริ่มเห็น
  และ 48 ใบชนขอบกระดาษ
- แก้เป็น `pageSize` = **9 × 5.5 นิ้ว** (ขนาดกระดาษจริง) แล้วกันขอบรูฟันเฟืองด้วย margin ข้างละ 0.5 นิ้ว
  — **พื้นที่พิมพ์ยังเป็น 8 × 5.1 นิ้วเท่าเดิม หน้าตาใบไม่เปลี่ยน**
- ขอบกระดาษย้ายจาก `@page` มาเป็น padding **ในตัวใบแต่ละใบ** เพื่อให้ทุกแผ่นมีขอบของตัวเอง
  (ของเดิมใส่ที่ container ซึ่งให้ขอบแค่หน้าแรกกับหน้าสุดท้าย)
- เอา `padding-top:0` ออกจากกฎ `.ot-sheet + .ot-sheet` ทั้งบนจอและตอนพิมพ์ —
  ไม่งั้นใบที่ 2 เป็นต้นไปจะเสียขอบบน 0.2 นิ้ว

**🐞 แก้ margin ที่ตั้งใน CSS ไม่ถูกใช้จริง**
- `webContents.print()` ไม่เคยส่ง `margins` ทำให้ Electron ใช้ `marginType: 'default'`
  = ขอบของ driver และ **มองข้าม `@page{margin}` ทั้งหมด**
  (ขอบซ้าย-ขวา 0.3 นิ้วที่แก้ไว้ใน v1.4.0 จึงไม่เคยทำงาน)
- ส่ง `margins: { marginType: 'none' }` แล้ว ตรงกับ driver ที่ตั้ง margin 0.00 ไว้พอดี

**คำเตือน "เนื้อหาเกินขอบกระดาษ" แม่นขึ้น**
- เดิมวัดเทียบกับความสูงรวมที่รวม padding ไปด้วย ทำให้พลาดกรณีเนื้อหาล้นจริง
  ตอนนี้วัดจาก content box — ใช้ได้ทั้งใบสั่งสินค้าและใบตรวจนับ

> ⚠️ การแก้ชุดนี้ยังไม่ผ่านการทดสอบกับ EPSON LQ-310 ตัวจริง — ควรพิมพ์ทดสอบ 3 ใบด้วย
> Microsoft Print to PDF ก่อน แล้วค่อยพิมพ์จริง 5 ใบ และ 20 ใบตามลำดับ

## v1.4.1

**ประวัติสต๊อก — แยก "ผู้นับ" ออกจาก "ผู้บันทึก"**
- แท็บ "รอบการตรวจนับ" เพิ่มคอลัมน์ **ผู้นับ (คนนับจริง)** แยกจาก **ผู้บันทึกเข้าระบบ**
  ตอบคำถาม "ใครเป็นคนนับของรอบนี้" ได้ตรงๆ (เดิมเห็นแค่คนที่คีย์ข้อมูล)
- หน้าต่าง "ดูรายการ" แสดงทั้งสองชื่อเช่นกัน
- ข้อมูลนี้ถูกเก็บใน `stocktake_sessions.counter_name` ตั้งแต่ v1.3.0 อยู่แล้ว
  รอบเก่าที่ไม่ได้กรอกชื่อผู้นับจะแสดงเป็น "—"

**เอกสาร**
- เพิ่ม `docs/HANDOFF.md` — สรุปสถานะโปรเจกต์ กติกาที่ยึดไว้ และงานที่ค้าง
  สำหรับใช้เปิดงานต่อในแชตใหม่โดยไม่ต้องเล่าประวัติซ้ำ

## v1.4.0

**หน้าตาใหม่ทั้งโปรแกรม** (จากดีไซน์ Claude Design)
- ตัวหนังสือพื้นฐาน 13.5px → **15px** · ตัวหนังสือรองเปลี่ยนจากเทาจาง `#6D767B` เป็น **`#3F4A4F`** อ่านง่ายขึ้นมากทุกหน้า
- ช่องกรอกและปุ่มสูง **44px** ขอบเข้มขึ้น · หัวตาราง 13.5px ตัวหนา · ช่องในตาราง 15px
- เมนูซ้ายกว้าง 236px แต่ละเมนูสูง 44px มี**จุดสถานะ** · แถบบนสูง 60px หัวข้อ 19px

**หน้าตรวจนับสต๊อก — ดีไซน์ใหม่**
- **ช่องกรอกใหญ่ 52px ตัวเลข 24px** · กด **Enter เลื่อนไปช่องถัดไป** คีย์รัวจากกระดาษได้
- **แถบความคืบหน้า** บอกคีย์แล้วกี่ช่อง / ผลต่างกี่รายการ / มูลค่าผลต่าง
- สถานะแต่ละแถวชัด (✅ ตรง / ⚠️ ต่าง / ◑ ไม่ครบ / ⬜ ยังไม่นับ) พร้อมแถบสีและพื้นหลัง
- ของเข้า/หมายเหตุยุบเป็นปุ่มกางออก — ไม่ต้องเลื่อนซ้าย-ขวา
- **ปุ่มบันทึกมี 2 จังหวะ** กันกดโดนโดยบังเอิญ
- คอลัมน์ **"รวมทุกคลัง"** ทั้งบนหน้าจอและบนกระดาษ
- ใบพิมพ์: **ตัวหนังสือดำล้วน** (เดิมหัวตารางเทาจาง พิมพ์ไม่ชัด)

**หน้าต่างพิมพ์ในตัวโปรแกรม** (แทนกล่องพิมพ์เก่าของ Windows)
- เลือกเครื่องพิมพ์ / จำนวนสำเนา / แนวกระดาษ พร้อม**ตัวอย่างกระดาษขนาดจริง 100%**
- **เตือนเมื่อเนื้อหาเกินขอบกระดาษ** (กรอบแดง + แถบเตือน) เห็นก่อนพิมพ์ ไม่ต้องเสียกระดาษลอง
- 🐞 **แก้บั๊กพิมพ์ไม่หยุด** — หน้าต่างตัวอย่างเป็น `position:fixed` ทำให้ Chromium พิมพ์ซ้ำทุกหน้า
  และตัวแอปเบื้องหลังยังกินพื้นที่ layout อยู่ แก้แล้วทั้งคู่ + เพิ่ม**เพดานจำนวนหน้า**
  (สั่งพิมพ์ 1 ใบ ส่งไปที่เครื่องพิมพ์ได้แค่ 1 หน้าเท่านั้น)

**ใบสั่งสินค้า**
- เลขที่เอกสาร **ตัดเดือนออก**: `A6908-001` → **`A69-0001`** (เลขรันทั้งปี ไม่รีเซ็ตรายเดือน, 4 หลัก)
- ขนาดกระดาษ **8 × 5.5 นิ้ว** (พื้นที่พิมพ์จริงของฟอร์ม 9 นิ้ว หักขอบรูฟันเฟือง 2 ข้าง)
- จัดใหม่ให้ **เต็มใบ** — ตารางยืดเต็มพื้นที่, แถวสูงขึ้น (8 แถว), ที่เซ็นชื่อมีระยะจริง
- **เส้นหนาขึ้นเป็น 1.5px** ทั้งใบ + กรอบนอกตาราง (เดิมเส้น 1px จางหายบน dot matrix)
- **ช่องรวมเงินใหญ่ขึ้น** — แถว "รวมเป็นเงิน (บาท)" เป็น 15px ตัวหนา
- ขอบซ้าย-ขวาเพิ่มเป็น 0.3 นิ้ว กันขอบขวาโดนตัด

**เอกสาร**
- เพิ่ม `docs/DESIGN-BRIEF-fullapp.md` + `docs/mockup-fullapp.html` (ชุดออกแบบทั้งโปรแกรม)

## v1.3.2

**หน้าต่างพิมพ์ในตัวโปรแกรม (แทนกล่องพิมพ์เก่าของ Windows)**
- แบ่งสองฝั่ง: ซ้ายเป็นตัวเลือก ขวาเป็น **ตัวอย่างกระดาษขนาดจริง** เลื่อนดูได้ทุกหน้า
- เลือก **เครื่องพิมพ์** ได้ในโปรแกรม (ดึงรายชื่อจาก Windows), ตั้ง **จำนวนสำเนา**, บอก **จำนวนแผ่นรวม**
- ใบตรวจนับเลือก **แนวตั้ง / แนวนอน** ได้ในหน้าต่างพิมพ์ เปลี่ยนแล้วตัวอย่างเปลี่ยนตามทันที และจำค่าที่เลือกไว้ให้
- กด "พิมพ์" แล้วพิมพ์ทันที ไม่มีกล่องพิมพ์ของ Windows เด้งขึ้นมา
  (ถ้าอยากได้ไฟล์ เลือกเครื่องพิมพ์เป็น Microsoft Print to PDF)
- ใบสั่งสินค้าใช้หน้าต่างเดียวกัน (ล็อกกระดาษ 9 × 5.5 นิ้ว)
- ย้ายตัวเลือกแนวกระดาษออกจากหน้าตรวจนับ เพราะซ้ำกับในหน้าต่างพิมพ์

## v1.3.1

**ปรับใบพิมพ์ตรวจนับให้เขียนง่ายขึ้น**
- **ตัดคอลัมน์ "หมายเหตุผู้นับ" ออกจากตาราง** — ตารางโล่งขึ้น ช่องอื่นกว้างขึ้น
- **ย้ายหมายเหตุไปไว้ท้ายกระดาษ** เป็นกล่อง "หมายเหตุ / เหตุผลของผลต่าง" มีที่ว่างให้เขียนหลายบรรทัด (มีทุกหน้า)
- **ขยายช่องวันที่และเวลาที่นับ** ให้เต็มความกว้างบรรทัด เขียนได้สบาย
- **เพิ่มความสูงทุกช่องเป็น 30px** และขยายช่อง "นับได้จริง" / "จำนวนของเข้า" / "วันที่ของเข้า"
- **ล็อกที่ใบละ 20 รายการ** — เกินกว่านั้นขึ้นกระดาษใหม่อัตโนมัติ พร้อมเลข **หน้า X / Y** และเลขลำดับเรียงต่อเนื่องข้ามหน้า

## v1.3.0

**ตรวจนับสต๊อก — ปรับใหญ่ให้ตรงกับการทำงานจริง**
- **ใบตรวจนับโชว์ทุกคลังในใบเดียว** — แต่ละสถานที่มีคอลัมน์คู่: *ในระบบ* (พิมพ์มาให้) + *นับได้จริง* (ช่องว่างให้เขียน)
  เปลี่ยนกระดาษเป็น **A4 แนวนอน** เพราะคอลัมน์เยอะขึ้น
- **เพิ่มช่อง "ของเข้า" 3 อย่าง** บนใบพิมพ์และในระบบ: ติ๊กว่ามีของเข้า / จำนวน / วันที่ของเข้า
  ใช้อธิบายผลต่างกรณีของเข้าวันนี้ หรือเข้ามาก่อนแต่ยังไม่ได้บันทึก
  (**เป็นข้อมูลประกอบเท่านั้น ไม่บวกสต๊อกเอง** — การรับของจริงยังทำที่หน้า "รับสินค้า" เพื่อไม่ให้นับซ้ำ)
- **เพิ่มคอลัมน์หมายเหตุผู้นับ** บนกระดาษ ให้คนนับเขียนเหตุผลเองได้
- **เลือกใบตรวจนับที่พิมพ์ไว้เพื่อกรอกย้อนหลังได้** — กดพิมพ์แล้วระบบจำใบนั้นไว้ (สถานะ "พิมพ์แล้ว")
  วันถัดไปมาคีย์ข้อมูล เลือกใบเดิมได้ **เลขที่เอกสารไม่เปลี่ยน** และช่องเลขที่ยังพิมพ์แก้เองได้
- **เพิ่มช่อง "ผู้นับ"** แยกจากคนที่คีย์ข้อมูลเข้าระบบ (ระบบยังเก็บคนคีย์ไว้ต่างหากเหมือนเดิม)
- ประวัติการตรวจนับแสดงสถานที่ + ข้อมูลของเข้าของแต่ละรายการแล้ว

**หน้าใหม่: ใบสั่งสินค้า** (กลุ่มเมนู "เอกสาร", สิทธิ์: คนจัดสต็อกขึ้นไป) — แทนการพิมพ์จาก Google Sheet
- **พิมพ์ฟอร์มเปล่าพร้อมเลขที่** สำหรับเขียนด้วยมือ — เลือกเล่ม A/B/C/D, เลขที่เริ่มต้น (แก้ได้), และจำนวนใบที่จะพิมพ์ทีเดียวหลายใบ
- เลขรันแยกตามเล่มและเดือน (`A6908-001`) เหมือนสูตรเดิมใน Apps Script — พิมพ์แล้วเดินเลขต่อให้เอง ไม่ซ้ำ
- พิมพ์ลงกระดาษต่อเนื่อง **9 × 5.5 นิ้ว** หนึ่งใบต่อหนึ่งแผ่น
- (เสริม) กรอกใบสั่งสินค้าในระบบแล้วพิมพ์ทั้งข้อมูลก็ได้ พร้อมเก็บประวัติและพิมพ์ซ้ำ
- **หมายเหตุ:** เลขที่ในหน้านี้แยกจากเลขที่ของ BillKeep — ถ้าใช้ทั้งสองโปรแกรม ควรใช้คนละเล่มกันเพื่อไม่ให้เลขชน

## v1.2.0

**หน้าใหม่: เชื่อมระบบบิล** (สิทธิ์: คนจัดสต็อกขึ้นไป) — ทำงานร่วมกับโปรแกรม **BillKeep** (ระบบเก็บบิลหน้าร้าน)
- **นำเข้าผลการปิดวัน** จากไฟล์ `.json` ของ BillKeep → ตัดสต๊อกเป็น movement แบบ `ISSUE`
  (`reference_type='bill'`) ตามบิลที่ขายไปจริง — สรุปให้ดูก่อนว่าจะตัดอะไร ข้ามอะไร แล้วค่อยยืนยัน
- **กันนำเข้าซ้ำ** — ไฟล์แต่ละชุดมีรหัส `batch_id` ที่ UNIQUE ในตาราง `bill_imports` นำเข้าซ้ำไม่ได้
- **สร้างสินค้าใหม่จากบิลอัตโนมัติ** — รายการที่ยังไม่มีในระบบจะถูกสร้างให้โดย **เว้นบาร์โค้ดไว้ก่อน**
  (ได้รหัสชั่วคราว `TMP-xxxxxx` + ธง "รอใส่บาร์โค้ด" แสดงในหน้าสินค้า) ธงจะปลดเองเมื่อแก้เป็นบาร์โค้ดจริง
- **ส่งออกรายการสินค้า** เป็นไฟล์ `.json` ให้ BillKeep (ใช้เมื่ออยู่คนละเครื่อง — ถ้าเครื่องเดียวกัน BillKeep อ่านฐานข้อมูลนี้แบบอ่านอย่างเดียวได้เลย)
- **ประวัติการนำเข้า** — ดูย้อนหลังได้ว่าไฟล์ไหนนำเข้าเมื่อไร ตัดกี่รายการ ข้ามกี่รายการ ใครนำเข้า

**ฐานข้อมูล:** เพิ่มตาราง `bill_imports` และคอลัมน์ `products.barcode_pending` (migration อัตโนมัติตอนเปิดโปรแกรม ข้อมูลเดิมไม่หาย)

## v1.1.3

**ประวัติสต๊อก — เก็บการตรวจนับแบบเต็มทุกรอบ**
- ทุกครั้งที่บันทึกการตรวจนับ = 1 รอบ เก็บ **รายการที่นับทั้งหมด** (แม้ตรงกับระบบ) ไม่ใช่แค่รายการที่เปลี่ยน
- เก็บ **2 เวลา** ต่อรอบ: เวลาที่คนตรวจนับ (จากใบนับ) + เวลาที่บันทึกเข้าระบบ
- หน้าประวัติสต๊อกแบ่งเป็น 2 แท็บ: **รอบการตรวจนับ** (ดูแต่ละรอบ + กด "ดูรายการ" เพื่อเทียบของที่นับ เช่น รอบเช้า vs รอบเย็น) และ **การเคลื่อนไหวทั้งหมด**
- รองรับทั้งการตรวจนับแบบมีเลขเอกสาร และแบบปรับด่วน (บันทึกเป็นรอบเหมือนกัน)

## v1.1.2

**ตรวจนับสต๊อก**
- เพิ่มตัวเลือก **บันทึกแบบไม่ออกเลขที่เอกสาร (ปรับด่วน)** — บันทึกการปรับสต๊อกโดยไม่ต้องมีเลขที่เอกสารและไม่ต้องพิมพ์ใบนับ (สำหรับการตรวจนับเร็วๆ ที่ไม่ใช้ใบนับ)

**หน้าใหม่: ประวัติสต๊อก**
- ดูประวัติการเคลื่อนไหวสต๊อกทั้งหมด (รับของ, ปรับสต๊อก, เบิกของ, สต๊อกตั้งต้น) 500 รายการล่าสุด
- แสดง วันที่-เวลาในระบบ, สินค้า, สถานที่, ประเภท, จำนวนที่เปลี่ยน, เลขที่อ้างอิง, หมายเหตุ, และผู้ทำรายการ
- ค้นหาและกรองตามประเภทได้ (สิทธิ์: คนจัดสต็อกขึ้นไป)

## v1.1.1

**ตรวจนับสต๊อก (Stocktake)**
- เพิ่ม **เลขที่เอกสารตรวจนับ** (serial number อัตโนมัติ เช่น `STK-20260724-0001`)
- เพิ่ม **วันที่ตรวจนับ** และ **เวลาที่นับจริง (ตามใบนับ)**
- เพิ่มปุ่ม **ดูตัวอย่าง / พิมพ์ใบตรวจนับ** — แสดงตัวอย่างบนจอก่อนพิมพ์
- ใบตรวจนับพิมพ์เป็นขนาด **A4** ตัวอักษรสีดำล้วน ไม่มีแถบสีพื้น
- ใบพิมพ์แสดง "พิมพ์เมื่อ" (เวลาที่พิมพ์อัตโนมัติ) + ช่องว่างให้เขียนเวลาที่นับจริงด้วยมือ
- ทุกการตรวจนับบันทึกเป็น session พร้อมเวลาในระบบ และผูก ADJUST movement กลับไปที่เลขเอกสาร

**สำรองและกู้คืนข้อมูล (Backup / Restore)** — หน้าผู้ใช้งาน (แอดมิน)
- **สำรองข้อมูล (Export)** — บันทึกฐานข้อมูลทั้งหมดเป็นไฟล์ `.db`
- **นำเข้าข้อมูล (Import)** — กู้คืน/ย้ายข้อมูลจากไฟล์สำรอง (ตรวจสอบไฟล์ก่อน + สำรองข้อมูลเดิมอัตโนมัติก่อนเขียนทับ)

## v1.1.0
- เพิ่มระบบตรวจนับสต๊อก serial number + พิมพ์ใบตรวจนับ (รุ่นแรก)

## v1.0.0
- รุ่นแรก: สินค้า, รับของ, เบิกของ, ตรวจนับ, แดชบอร์ด, ผู้ใช้งาน, ระบบสิทธิ์ 3 ระดับ, stock ledger
